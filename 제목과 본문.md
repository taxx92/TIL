# HTML

## 제목과 본문

### HTML 문서 시작하기

1. VS Code에서 `파일` - `폴더 열기` (원하는 폴더 생성 후 지정)
2. `index.html`파일 생성

```
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
</body>
</html>
```
**Emmet**명령어 `!` 입력 후 탭 : HTML 기본 틀
  * `lang`의 `en`을 `ko`로 수정
  * `<body>` 안쪽에 페이지 요소들 작성
  * HTML 파일 우클릭 - Open with Live Server 이용하면 미리보기 가능 (확장파일 추가해야 함)

### 태그

|태그 또는 구문|설명|비고|
|--|--|--|
|`<h1>` ~ `<h6>`|제목|숫자가 낮을 수록 높은 단계|
|`<p>`|문단|각각 줄바꿈이 됨|
|`<br>`|줄바꿈|닫는 태그 필요 없음. `<br/>`, `<br />`와 혼용되기도 함|
|`<hr>`|가로줄|닫는 태그 필요 없음|
|`&nbsp;`|공백(스페이스)|스페이스를 강제할 떄 사용|

### 주석

개발자에게만 보이는 텍스트

주석으로 처리할 부분을 선택한 뒤
* 윈도우 : `crtl` + `/`
* 맥 : `Command` + `/`
