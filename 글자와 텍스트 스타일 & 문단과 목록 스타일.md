## 글자와 텍스트 스타일

### CSS 기본 문법

```
선택자 {
  속성1: 값;
  속성2: 값;
  /* ... */
}
```

### 속성들

`font-stlye` 속성 : 글자를 기울일 때 사용 (normal, italic)

`font-weight` 속성 : 글자의 굵기 조절 (nomal, bold)

`font-size` 속성 : 글자의 크기 지정 (절대값 px, 100% = 1em, rem은 중첩영향x)

`text-decoration` 속성 : 밑줄, 취소선, 물결선 등으로 글을 꾸며줌 (underline, line-through, wavy underline tomato 등등)

`text-transform` 속성 : 알파벳의 대소문자 표시 (capitalize(첫글자만 대문자), uppercase(대문자), lowercase(소문자))

`text-shadow` 속성 : 텍스트에 그림자를 줄 수 있음 ('x좌표, y좌표, 흐림(선택), 색' 형식으로 넣을 수 있음, 크롬 계열 브라우저의 개발자 도구 사용하면 손쉽게 조정)

크롬 개발자 도구 열기

윈도우 : `ctrl` + `shift` + `i`
맥 : `cmd` + `alt` + `i`


---

## 문단과 목록 스타일

### 속성들

`text-align` 속성 : 텍스트의 정렬 방식을 지정(left, center, right, justify(양쪽정렬))

`letter-spacing` 속성 : 자간

'word-spacing` 속성 : 단어 간격

`line-height` 속성 : 줄 높이

`text-indent` 속성 : 들여쓰기 조절

`list-style` 속성 : `ul`, `ol` 목록의 불릿, 숫자 스타일 지정

---

참고강의 - [얄코(Yalco) <제대로 파는 HTML CSS>](https://www.inflearn.com/course/%EC%A0%9C%EB%8C%80%EB%A1%9C-%ED%8C%8C%EB%8A%94-html-css/dashboard)
