# ✨HTML

## 제목과 본문

### HTML 문서 시작하기

1. VS Code에서 `파일` - `폴더 열기` (원하는 폴더 생성 후 지정)
2. `index.html`파일 생성

```
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
</body>
</html>
```
**Emmet**명령어 `!` 입력 후 탭 : HTML 기본 틀
  * `lang`의 `en`을 `ko`로 수정
  * `<body>` 안쪽에 페이지 요소들 작성
  * HTML 파일 우클릭 - Open with Live Server 이용하면 미리보기 가능 (확장파일 추가해야 함)

### 태그

|태그 또는 구문|설명|비고|
|--|--|--|
|`<h1>` ~ `<h6>`|제목|숫자가 낮을 수록 높은 단계|
|`<p>`|문단|각각 줄바꿈이 됨|
|`<br>`|줄바꿈|닫는 태그 필요 없음. `<br/>`, `<br />`와 혼용되기도 함|
|`<hr>`|가로줄|닫는 태그 필요 없음|
|`&nbsp;`|공백(스페이스)|스페이스를 강제할 떄 사용|

### 주석

개발자에게만 보이는 텍스트

주석으로 처리할 부분을 선택한 뒤
* 윈도우 : `crtl` + `/`
* 맥 : `Command` + `/`

---

## 종류와 중요도에 따른 태그

### 강조하기 위한 태그들

|태그|설명|
|--|--|
|`<b>`|글자를 굵게|
|`<strong>`|중요한 내용임을 명시|
|`<i>`|글자를 기울임|
|`<em>`|강조할 내용임을 명시|

b 태그와 strong 태그는 브라우저에서는 같아 보이지만 다른 태그

i 태그와 em 태그 도 마찬가지

HTML 태그는 정보의 **종류**를 구분하는데만 사용
(오늘날 스타일 정보는 **CSS**로 분리)


### 첨자 태그

|태그|설명|비고|
|--|--|--|
|`<sup>`|위 첨자|지수, 서수|
|`<sub>`|아래 첨자|각주, 변수, 화학식|

### (구)밑줄 태그와 취소선 태그

|태그|설명|비고|
|--|--|--|
|`<u>`|철자 오류 표시|예전에는 밑줄 용도로 사용|
|`<s>`|더 이상 유효하지 않은 정보 표시||

---

참고강의 - [얄코(Yalco) <제대로 파는 HTML CSS>](https://www.inflearn.com/course/%EC%A0%9C%EB%8C%80%EB%A1%9C-%ED%8C%8C%EB%8A%94-html-css/dashboard)
